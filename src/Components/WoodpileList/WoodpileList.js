import React from 'react';

const BlogList = ({data}) =>{

    return (
        <div className={styles.blogList}>
            {
                
            }
        </div>
    );

}

export default BlogList;